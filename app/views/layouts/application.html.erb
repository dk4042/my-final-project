<!DOCTYPE html>
<html>
  <head>
    <title>Rails Template</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <style>
      body {
        font-family: 'Arial', sans-serif; /* Simple web-safe font */
        line-height: 1.6;
        margin: 0;
        padding: 0;
        background-color: #f4f4f4; /* Soft background color */
      }

      nav {
        background-color: #D2B48C; /* Light brown background */
        padding: 15px;
        text-align: center;
      }

      nav a {
        color: white;
        text-decoration: none;
        margin: 0 15px; /* Add space between links */
        font-size: 16px;
        font-weight: bold; /* Make text bold for emphasis */
        transition: color 0.3s;
      }

      nav a:hover {
        color: #3d2a15; /* Dark brown on hover */
      }

      nav a.active {
        color: #3d2a15; /* Dark brown for active link */
      }

      /* Styling for better spacing and readability */
      h1, p {
        text-align: center;
        margin-bottom: 20px;
        padding: 0 10px;
        color: #3d2a15;
      }

      /* Add padding to links for better spacing */
      a {
        padding: 10px 20px;
        border-radius: 5px;
        margin-top: 10px;
        display: inline-block;
      }

      a:hover {
        background-color: #B08D57;
        color: white;
      }
    </style>
  </head>

  <body>
    <nav>
      <a href="/" class="<%= 'active' if request.path == '/' %>">Homepage</a>
      <a href="/random_review" class="<%= 'active' if request.path == '/random_review' %>">Lucky Review</a>
      <a href="/coffee_reviews">Coffee Reviews</a>

      <% if current_user != nil %>
        <a href="/users/sign_out">Sign Out</a>
        <a href="/users/edit">Edit profile</a>
      <% else %>
        <a href="/users/sign_up">Sign Up</a>
        <a href="/users/sign_in">Sign In</a>
      <% end %>
    </nav>

    <%= yield %>
  </body>
</html>
